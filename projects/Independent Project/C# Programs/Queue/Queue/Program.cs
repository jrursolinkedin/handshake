using System;

namespace Queue{
    class Program{
        static void Main(string[] args){
            /**
             *  Queue:
             *  A queue is a linear data structure which follows a 
             *  particular order in which the operations are performed.
             *  The order is First In First Out (FIFO).
             *  
             *    Enqueue --> [] [] [] [] [] [] --> Dequeue
             *                ^               ^
             *                Rear           Front
             *                
             *   Queue Methods:
             *   - Enqueue: Adds a node to the rear of the list.
             *   - Dequeue: Deletes a node from the front of the list.
             *   - Peek(): Gets the value at the front of the list.
             *   - Size(): Gets the number of nodes in the list.
             *   - IsEmpty(): If the list is empty, it returns true.
             *   - Clear(): Deletes all the nodes from the list.
             */
        }
    }
}
