using System;

namespace Priority_Queue{
    class Program{
        static void Main(string[] args){
            /**
             *  Priority Queue:
             *  A priority queue is an extension of queue with following
             *  properties:
             *   * Every item has a priority associated with it.
             *   * An element with high priority is dequeued before an
             *     element with low priority.
             *   * If two elements have the same priority, they are served
             *     according to their order in the queue.
             *     
             *                   Priority Queue:
             *      [10 , 9 , 8 , 7 , 6 , 5 , 0 , 0 , 0 , 0]
             *      max = 10;
             *      nItems = 6;
             *      
             *  Priority Queue Methods:
             *  - Push(int val): Adds an integer to the array.
             *  - Pop(): Removes an integer with highest priority from array.
             *  - Peek(): Returns an integer with the highest priority from array.
             *  - Size(): Returns the number of integers within the array.
             *  - IsFull(): If the array is filled, it returns true.
             *  - IsEmpty(): If the array is empty, it returns true.
             */
        }
    }
}
