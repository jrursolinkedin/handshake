using System;

namespace Stack{
    class Program{
        static void Main(string[] args){
            /**
             *  Stack:
             *  A stack is a linear data structure which follows a
             *  particular order in which the operations are performed. 
             *  The order may be LIFO (Last In First Out) or FILO 
             *   (First In Last Out).
             *                          /<-- Push()
             *   [] [] [] [] [] [] <---
             *   ^               ^      \--> Pop()
             *   Bottom         Top       
             *   
             *   Stack Methods:
             *   - Push(object val): Adds a node to the top of the list.
             *   - Pop(): Deletes a node from the top of the list.
             *   - Peek(): Gets the value of the top node.
             *   - Size(): Gets the size of the list.
             *   - IsEmpty(): If the list is empty, it returns true.
             *   - Clear(): Deletes the list.
             */ 
        }
    }
}
